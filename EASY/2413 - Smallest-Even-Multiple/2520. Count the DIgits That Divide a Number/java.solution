class Solution {
    public int countDigits(int num) {
        int original = num;
        int cnt = 0;
       while(num>0){
        int ld = num%10;
        if(ld!=0&&original%ld==0){
            cnt++;
        }
        num = num/10;
        }
        return cnt;            
    }
}